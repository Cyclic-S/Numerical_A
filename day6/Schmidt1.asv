function s=Schmidt1(a,b,rho,n)
%第n项Gram-Schmidt多项式
syms x
sum=0;
P=[1,x];
for i=2:n
    P=[P,x^i];
end
for i=2:n
    for k=1:i
        sum=sum+int(rho*x^i*P(k),a,b)/int(rho*P(k)*P(k),a,b)*P(k);
    end
    P(i+1)=x^i-sum;
end
s=P;